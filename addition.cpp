#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a[] = {1, 2, 3};
    int b[] = {3, 2, 1};
    int sum[10];
    int topa = sizeof(a) / sizeof(a[0]);
    int topb = sizeof(b) / sizeof(b[0]);
    int tops = 0;
    int carry;
    for (int i = 0; topa == 0 && topb == 0; i++)
    {
        int sum=
    }
}